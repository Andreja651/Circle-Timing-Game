// Update this page (the content is just a fallback if you fail to update the page)
import CircleReflexGame from '@/components/CircleReflexGame';

const Index = () => {
  return <CircleReflexGame />;
};

export default Index;
